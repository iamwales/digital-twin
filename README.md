# Digital Twin

Personal chat “twin”
 **Frontend** (Next.js), 
 **backend** (FastAPI / Lambda), 
 **infra** (Terraform on AWS: API Gateway, Lambda, S3, CloudFront).

**Deploy:** GitHub Actions on `main`, or locally with `./scripts/deploy.sh`
See `terraform/` and `.github/workflows/`.

